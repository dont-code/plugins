import { BeautifierPipe } from './beautifier.pipe';

describe('BeautifierPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BeautifierPipe();
    expect(pipe).toBeTruthy();
  });
});
